node('master'){
    stage('Docker'){
        println "hello, world!"
    }

    stage('Grab Artifact'){
        copy()
    }
}

def copy(){
    step([$class: 'CopyArtifact', fingerprintArtifacts: true, parameters: 'Code/**/**/*.war', projectName: 'multi/master', selector: [$class: 'StatusBuildSelector', stable: false], target: 'output'])

}