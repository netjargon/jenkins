node('master'){
    stage('Docker'){
        println "hello, world!"
    }

    stage('Grab Artifact'){
        step([$Class: 'CopyArtifact',
                projectName: 'test',
                filter: 'output/**/*.war']);
    }
}